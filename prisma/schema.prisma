generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model cliente {
  id       String @id @default(auto()) @map("_id") @db.ObjectId
  email    String
  nome     String
  telefone String
}

model funcionario {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  cargo     String
  cpf       String
  data_nasc DateTime @db.Date
  email     String
  idade     Float
  nome      String
  root      Boolean
  senha     String
  telefone  String
}

model vaga {
  id             String @id @default(auto()) @map("_id") @db.ObjectId
  clienteId      String
  dataLocacao    String
  dataLocacaoFim String
  nome           String
  preco          Float
  setor          String
  tipo           String
  vaga_ocupada   String
}

model veiculo {
  id        String @id @default(auto()) @map("_id") @db.ObjectId
  categoria String
  cor       String
  modelo    String
}
